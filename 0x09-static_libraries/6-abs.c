#include "main.h"
/**
* _abs - check code
* @n: An integre input
* Return: Absolut value of number r
*/
int _abs(int n)
{
if (n > 0 || n == 0)
{
return (n);
}
else
return (n * -1);
}























































































