#include "main.h"
/**
 * _sqrt_recursion - calculate the root of  natural squareroot.
 * @n: number to calculate squareroot
 * @i: iterate number
 * Return: the natural squareroot
 */
int _sqrt_recursion(int n)
{
if (n < 0)
return (-1);
{
if (sqrt == n)
return (1);
}
return (_sqrt(n+ 1));
}
